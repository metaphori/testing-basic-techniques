name: workflow jvm
on:
  push:
    paths: 'jvm/**'
jobs:
  run-tests:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - run: |
            cd jvm
            ./gradlew test
